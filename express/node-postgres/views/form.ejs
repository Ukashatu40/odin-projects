<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Search</title>
</head>
<body class="bg-gradient-to-r from-green-100 to-teal-100 min-h-screen">
    <header class="mb-8 pt-6">
        <h1 class="text-4xl font-semibold text-teal-800 text-center">Search Users</h1>
      </header>
    <form action="/" method="get" class="bg-white p-6 rounded-lg shadow-lg max-w-md mx-auto space-y-4">
        <div class="mb-6">
          <input type="search" name="search" placeholder="Search users by Name or Email..." class="w-full p-3 border border-teal-300 rounded-lg focus:ring-2 focus:ring-teal-500" value="<%= typeof query !== 'undefined' ? query : '' %>">
        </div>
      </form>

      <h2 class="text-2xl font-semibold text-teal-800 text-center mt-8 mb-4">Create User</h2>
      <form action="/" method="POST" class="bg-white p-6 rounded-lg shadow-lg max-w-md mx-auto space-y-4">
        <div class="mb-6">
          <input type="text" name="username" placeholder="Search users by Name or Email..." class="w-full p-3 border border-teal-300 rounded-lg focus:ring-2 focus:ring-teal-500" value="<%= typeof query !== 'undefined' ? query : '' %>">
        </div>
        <div class="mb-6">
          <button type="submit" class="w-full bg-teal-500 text-white px-4 py-2 rounded-lg hover:bg-teal-600 transition-colors">Submit</button>
        </div>
      </form>
</body>
</html>