<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
  <title><%= title %></title>
</head>
<body class="bg-gradient-to-r from-green-100 to-teal-100 min-h-screen">
  <div class="container mx-auto p-6">
    <header class="mb-8">
      <h1 class="text-4xl font-semibold text-teal-800 text-center"><%= title %></h1>
    </header>
    
    <% if (locals.users && users.length > 0) { %>
      <ul class="space-y-3">
        <% users.forEach(user => { %>
          <li class="bg-white p-5 rounded-lg shadow-lg flex flex-col md:flex-row items-start md:items-center justify-between border-l-4 border-teal-500 hover:shadow-xl transition-shadow">
            <div class="mb-2 md:mb-0">
              <p class="text-lg font-medium text-teal-800">ID: <%= user.id %></p>
              <p class="text-lg text-gray-700">Name: <%= user.firstName %> <%= user.lastName %></p>
              <p class="text-sm text-gray-600">Email: <%= user.email %></p>
              <p class="text-sm text-gray-600">Age: <%= user.age %></p>
              <p class="text-sm text-gray-600">Bio: <%= user.bio %></p>
            </div>
            <div class="flex flex-col md:flex-row space-y-2 md:space-y-0 md:space-x-4">
              <a href="/<%= user.id %>/update" class="text-teal-600 hover:text-teal-800 font-semibold px-4 py-2 rounded hover:bg-teal-100">Update</a>
              <form action="/<%= user.id %>/delete" method="POST" style="display:inline;">
                <button type="submit" class="text-red-600 hover:text-red-800 font-semibold px-4 py-2 rounded hover:bg-red-100" onclick="return confirm('Are you sure you want to delete this user?');">Delete</button>
              </form>
            </div>
          </li>
        <% }); %>
      </ul>
    <% } else { %>
      <p class="text-center text-gray-600">No users found.</p>
    <% } %>
    
    <a href="/create" class="mt-8 block w-full max-w-xs mx-auto bg-teal-500 text-white px-6 py-3 rounded-lg hover:bg-teal-600 text-center font-medium transition-colors">Create a user</a>
  </div>
</body>
</html>